name: Display Current Time

on:
  workflow_dispatch:

jobs:
  show-time:
    runs-on: ubuntu-latest
    steps:
      - name: Print current UTC time
        run: date -u

      - name: Print Variable
        run: echo ${{vars.MY_VAR}}

      - name: Print SECRET
        run: echo ${{secrets.AABBCC}}
